# Sequence = which are indexable and we can iterate through them.
# Sequence Types :
#     1. Mutable Sequence Types : 
#             . Change after creation
#             . We can Modify, Add, Remove element
#             . Ordered

#         i. List : A list is a mutable, ordered collection of items.
#         ii. Byte Arrays : Byte array is a mutable, ordered sequences of bytes.
#         iii. Arrays : Similar to list, Mutable, Ordered coolection of items but optimized for numeric operations and storing elements of the same data types.
#           iv. Dictionary : A pair of {key : value}
#            v. Set : 

#     2. Immutable Sequence Types :
#             .
#             .
#             .
        
#         Tuples: Collections of objects that are ordered and immutable.
#         Strings: Collection of Character sequences separated by single quotes ('') or double quotes ("") are referred to as strings.
#         Integers: Integers are defined as Whole numbers, both positive and negative, and are represented as integers, an immutable data type.
