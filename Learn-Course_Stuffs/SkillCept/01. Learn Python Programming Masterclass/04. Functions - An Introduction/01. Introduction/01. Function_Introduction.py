# Function : reusable block of code designed to perform a specific task
# Method : A function that's bound to an instance of a class
# Structure of function
# def my_func(a, b):
#   body
#   return answer

    # def : keyword used to define function
    # my_func : name of the function
    # (a, b) : every function have (), and a&b are parameters
    # : : colon used to end the definition of function
    # then body of the function
    # return : keyword used to take output from the function
    # after each funtion end there should must be 2 blank line as per PEP8 convention

# To call the function : my_func()
